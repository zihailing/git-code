#/bin/sh
git init
git add (the files)
git status
git commit -m "the description of the files"
git remote add origin git@github.com:zihailing/ChIP-seq.git
git push -f origin
###############resolve the error report######################
git remote set-url origin ssh://github.com/zihailing/ChIP-seq.git
git push
################checkout different version###################
git log
git checkout XXXX(commit)
git checkout master(branch)
